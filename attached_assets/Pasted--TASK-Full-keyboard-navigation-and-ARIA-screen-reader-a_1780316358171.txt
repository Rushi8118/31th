# TASK: Full keyboard navigation and ARIA/screen-reader audit

## Context
React + Vite. Site is Siddhivinayak Overseas — a visa consultancy.
Audit and fix keyboard + screen reader accessibility across all interactive
elements. Follow WCAG 2.1 AA standard throughout.

# Fix every item found. Do NOT document issues without fixing them.
# Output complete file contents for every modified file.

## Keyboard Navigation Fixes

Tab order:
[ ] Verify Tab key moves focus in logical visual order: logo → nav links →
    Login → Register → Free Consultation → hero CTA buttons → rest of page.
[ ] No focus traps outside of modals/dialogs.
[ ] Skip-to-content link: add <a href="#main-content" className="sr-only focus:not-sr-only">
    as the very first element in the DOM. Style it to appear on focus
    using the gold color (#c9a227) as outline.

Button/link behavior:
[ ] All <button> elements must activate on Enter AND Space.
[ ] All nav links must activate on Enter.
[ ] "Free Consultation" CTA: activates on Enter.

Hamburger menu (mobile):
[ ] Toggle button: aria-expanded="true/false", aria-controls="mobile-menu",
    aria-label="Open navigation menu"
[ ] When menu opens: focus moves to first menu item.
[ ] Escape key: closes menu, returns focus to hamburger button.
[ ] Tab through all menu items, Tab past last item closes menu.

Dropdowns (if any on Work Visa / Study Visa):
[ ] aria-haspopup="true", aria-expanded on trigger button.
[ ] Arrow Down: opens dropdown, Arrow Up/Down: navigate items.
[ ] Escape: closes dropdown, returns focus to trigger.

Modals/dialogs:
[ ] On open: focus moves to modal, background scroll locked.
[ ] role="dialog", aria-modal="true", aria-labelledby pointing to modal title.
[ ] Focus is TRAPPED inside modal (Tab cycles within modal only).
[ ] Escape closes modal, focus returns to trigger element.

## ARIA Labels & Screen Reader Fixes

Navbar logo:
[ ] <a aria-label="Siddhivinayak Overseas — home page">
[ ] Logo image: alt="Siddhivinayak Overseas logo"

Navigation:
[ ] Wrap nav links in <nav aria-label="Main navigation">
[ ] Active page link: aria-current="page" on the Home link

Hero section:
[ ] Main landmark: <main id="main-content"> wrapping everything below navbar
[ ] Trust badge: aria-label="Trusted by 5,000 plus aspirants worldwide"
    (screen readers will mispronounce "5,000+" without this)
[ ] 3D Earth canvas: aria-hidden="true" (it's decorative — screen readers
    should skip it entirely)
[ ] Floating stat card: aria-label="Average processing timeline: 8 to 12 weeks. Fast-track routes available."

CTA Buttons:
[ ] "Start Your Visa Journey →": aria-label="Start your visa journey with Siddhivinayak Overseas"
[ ] "Explore Countries": keep as-is, it's clear.

Stats bar (98%, 10+):
[ ] <span aria-label="98 percent visa success rate">98%</span>
[ ] <span aria-label="10 plus countries served">10+</span>

Focus styles:
[ ] Every focusable element must have a visible focus ring.
    Use: outline: 2px solid #c9a227; outline-offset: 3px (gold — matches brand)
    NEVER: outline: none without a replacement visible focus indicator.
[ ] Add to global CSS: :focus-visible { outline: 2px solid #c9a227; outline-offset: 3px; }

Color contrast:
[ ] Verify gold text (#c9a227) on cream (#f5f0e8) meets WCAG AA (4.5:1 ratio).
    If it fails (it likely does for small text), darken gold to #a07d1a for body
    text use cases only. Keep #c9a227 for large headings only.
[ ] Navy (#1a2340) on cream: verify passes (it will — dark on light is fine).

## Acceptance criteria
- Full page navigable by Tab key alone, in logical order
- Every interactive element has a visible focus ring
- Screen reader announces all meaningful content correctly
- Decorative elements (globe, star animation) are aria-hidden
- Hamburger menu fully keyboard operable
- Modals trap focus correctly
- 0 axe-core or WAVE tool errors at AA level