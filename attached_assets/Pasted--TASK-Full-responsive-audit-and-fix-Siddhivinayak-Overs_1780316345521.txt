# TASK: Full responsive audit and fix — Siddhivinayak Overseas React site

## Context
React + Vite + Tailwind CSS. The site has these sections:
navbar, hero (text left + 3D globe right + floating stat card),
trust badges row, and further sections below.
The brand colors are: cream bg #f5f0e8, navy #1a2340, gold #c9a227.

# Test at exactly these viewport widths: 320px, 480px, 768px, 1024px, 1440px
# For EACH width: document every broken element, then immediately fix it.
# Output each fixed file in full — no partial snippets.

## Breakpoint 1 — 320px (small mobile)

Check and fix:
[ ] Navbar: logo + brand name must fit on one line. Nav links collapse to
    hamburger menu. "Free Consultation" button must not overflow.
    Login and Register links must be inside the hamburger menu at this size.
[ ] Hero: switch to single column layout (globe below text, not side-by-side).
    Globe canvas: 280×280px, centered.
    Floating "8-12 weeks" card: repositioned below globe, not overlapping.
    Headline font-size: clamp(28px, 8vw, 36px) — never wraps to 4+ lines.
[ ] "Start Your Visa Journey" button: full width (100%), not truncated.
[ ] Trust badges row: wraps to 2 rows, no horizontal scroll.
[ ] Stats bar (98%, 10+): stacks vertically, text remains readable.

## Breakpoint 2 — 480px (large mobile)

[ ] Same layout as 320px but globe: 300×300px.
[ ] CTA buttons: side-by-side if they fit, stacked if not.
[ ] Check for any horizontal scrollbar — fix any element causing overflow.

## Breakpoint 3 — 768px (tablet)

[ ] Navbar: consider whether nav links fit or still need hamburger.
    If links fit, show them. If not, keep hamburger.
[ ] Hero: two-column layout can begin here. Globe: 360×360px.
    Floating stat card: positioned bottom-right of globe, not off-screen.
[ ] Verify the globe circular clip frame stays perfectly round (not oval).
[ ] Check all section padding — minimum 24px horizontal padding on tablet.

## Breakpoint 4 — 1024px (small desktop / laptop)

[ ] Full desktop layout. Globe: 440×440px.
[ ] Navbar: all links visible. No hamburger.
[ ] Hero text: headline at designed size. No line-height issues.
[ ] Floating stat card: correct position, correct z-index (on top of globe).
[ ] Verify no element extends beyond the viewport width.

## Breakpoint 5 — 1440px (large desktop)

[ ] Hero section: max-width container (e.g. 1280px) centered, with
    auto side padding. No content stretches to full 1440px width awkwardly.
[ ] Globe: 520×520px max. Does not push text out of frame.
[ ] All font sizes at their maximum designed values.

## Global fixes to apply at all breakpoints

[ ] Add overflow-x: hidden to html and body — prevent horizontal scroll
[ ] All images: max-width: 100%, height: auto
[ ] Touch targets: all buttons and links must be minimum 44×44px tap area
[ ] The navbar must use position: sticky; top: 0; z-index: 50 — it should
    stay visible when scrolling at all screen sizes
[ ] Mobile hamburger menu: must be closable by clicking outside (click-away listener)
    and by pressing Escape key

## Acceptance criteria
- Zero horizontal scrollbar at any tested width
- No text truncated or overflowing its container
- Globe always circular, never oval or clipped
- Floating stat card always readable, never off-screen
- All buttons tappable on mobile (≥44px touch area)
- Hamburger menu works open/close on mobile